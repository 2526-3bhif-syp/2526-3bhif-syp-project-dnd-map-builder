---
phase: quick
plan: 260408-i90
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
  - src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java
autonomous: true
requirements: []
must_haves:
  truths:
    - Map centers automatically after a new map is generated
  artifacts:
    - path: src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
      provides: "centerMap method"
    - path: src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java
      provides: "Call to centerMap in renderMap"
  key_links: []
---

<objective>
Make the map center itself automatically after regeneration by resetting its translation and scale relative to its container.
</objective>

<context>
@src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
@src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add centerMap method to MainView</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainView.java</files>
  <action>
    - Make the `Group canvasGroup` currently defined as a local variable in `setupCanvasContainer()` a private field in `MainView` so it can be accessed.
    - Add a new public method `centerMap()` to `MainView`.
    - Inside `centerMap()`, calculate the centered X and Y coordinates:
      - `double cw = canvasContainer.getWidth();`
      - `double ch = canvasContainer.getHeight();`
      - `double mw = canvas.getWidth();`
      - `double mh = canvas.getHeight();`
      - `double tx = cw > 0 ? (cw - mw) / 2 : 0;`
      - `double ty = ch > 0 ? (ch - mh) / 2 : 0;`
    - Reset the scale of `canvasGroup` back to 1.0 (`setScaleX(1.0)`, `setScaleY(1.0)`).
    - Set the translation of `canvasGroup` to `tx` and `ty` (`setTranslateX(tx)`, `setTranslateY(ty)`).
  </action>
  <verify>
    <automated>grep -q "public void centerMap" src/main/java/com/mapbuilder/mapbuilder/main/MainView.java</automated>
  </verify>
  <done>centerMap method exists in MainView and correctly calculates translation</done>
</task>

<task type="auto">
  <name>Task 2: Call centerMap after generation</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java</files>
  <action>
    - Open `MainPresenter.java` and find the `renderMap()` method.
    - At the end of `renderMap()`, after `pixelWriter.setPixels(...)`, call `view.centerMap()`.
    - Also, to ensure the map centers on the initial layout, add a listener in `MainView` or `MainPresenter`. The simplest way is to also add a layout listener to `canvasContainer` in `MainView` that fires once on the first valid layout. But calling `view.centerMap()` in `renderMap()` fulfills the main requirement "after regeneration". We can just add `Platform.runLater(() -> view.centerMap());` at the end of `renderMap()` to ensure UI layout bounds are ready if needed, or simply `view.centerMap();`. Use `javafx.application.Platform.runLater(() -> view.centerMap());` to ensure layout has happened.
  </action>
  <verify>
    <automated>grep -q "centerMap()" src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java</automated>
  </verify>
  <done>centerMap is invoked at the end of rendering</done>
</task>

</tasks>

<success_criteria>
Generating a new map resets the canvas to 1.0 zoom and centers it within the canvas container.
</success_criteria>
