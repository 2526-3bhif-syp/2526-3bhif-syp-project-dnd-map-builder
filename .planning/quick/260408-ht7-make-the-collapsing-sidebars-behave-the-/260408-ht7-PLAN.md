---
phase: quick
plan: 260408-ht7
type: execute
wave: 1
depends_on: []
files_modified: ["src/main/java/com/mapbuilder/mapbuilder/main/MainView.java"]
autonomous: true
requirements: ["UI-FIX-01"]
must_haves:
  truths:
    - "Left sidebar is full screen height without any 1px offsets or borders"
    - "Left sidebar collapse behavior matches right sidebar"
  artifacts:
    - path: "src/main/java/com/mapbuilder/mapbuilder/main/MainView.java"
      provides: "Main view UI"
  key_links: []
---

<objective>
Fix the 1px offset on the left sidebar by removing default ScrollPane borders and ensure the collapsing behavior matches the right sidebar.
Purpose: Polish the UI to eliminate visual artifacts and provide consistent interactions.
Output: Updated MainView.java with correct styling and transition offsets.
</objective>

<context>
@.planning/STATE.md
@src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 1px offset and styling on the left panel</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainView.java</files>
  <action>
    Modify `leftScroll` styling in `setupLeftPanel` to eliminate the default 1px border.
    Set the style to: `"-fx-background: #2b2b2b; -fx-background-color: #2b2b2b; -fx-padding: 0; -fx-border-color: transparent; -fx-border-width: 0; -fx-background-insets: 0;"`.
    Ensure the left panel's VBox (`leftPanel`) also has `-fx-padding: 0;` or similar if needed, but its current padding is fine. 
    Ensure `showLeftBtn` is correctly positioned on the left edge.
  </action>
  <verify>
    <automated>grep -q "border-color: transparent" src/main/java/com/mapbuilder/mapbuilder/main/MainView.java</automated>
  </verify>
  <done>Left panel has no default borders causing a 1px gap.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure identical collapse behavior</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainView.java</files>
  <action>
    Review the `TranslateTransition` in `setupLeftPanelShowButton`. The `leftScroll` should hide completely, including its dropshadow.
    Currently it translates to `-280`. Ensure `showLeftBtn` appearance matches the right side (where `showRightBtn` appears instantly when animation finishes).
    No major changes needed, but confirm `showLeftBtn` has `AnchorPane.setLeftAnchor(showLeftBtn, 0.0);` and matches the right button's animation logic.
  </action>
  <verify>
    <automated>grep -q "TranslateTransition" src/main/java/com/mapbuilder/mapbuilder/main/MainView.java</automated>
  </verify>
  <done>Collapse behaviors are fully aligned between left and right sidebars.</done>
</task>

</tasks>

<verification>
Review the UI rendering to confirm that left and right sidebars look and animate identically (mirrored) and no 1px gap exists on the left edge.
</verification>

<success_criteria>
Left sidebar is flush with the screen edge, animates smoothly off-screen, and shows the reveal button consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/quick/260408-ht7-SUMMARY.md`
</output>