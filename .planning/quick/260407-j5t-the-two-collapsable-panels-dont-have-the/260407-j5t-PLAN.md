---
phase: quick
plan: 260407-j5t
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
  - src/main/resources/styles.css
autonomous: true
requirements: [QUICK-01]
must_haves:
  truths:
    - Left and right collapsible panels have consistent start states and behavior
    - Sliders match the dark UI theme
  artifacts:
    - path: src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
      provides: Consistent panel toggle logic
    - path: src/main/resources/styles.css
      provides: Dark theme styles for sliders
  key_links: []
---

<objective>
Fix the open state of the two collapsable panels so they match the layer panel's behavior, and theme the sliders to match the dark UI.
</objective>

<context>
@src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
@src/main/resources/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align Left Panel behavior with Layer Panel</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainView.java</files>
  <action>
    Review the initialization and transition states of `leftScroll` (Left Panel) and `layersPanel` (Right Panel).
    Adjust the translation and visibility logic of `collapseLeftBtn` and `showLeftBtn` so the Left Panel behaves exactly like the Layer Panel. If the Layer Panel is intended to be open by default or closed by default, ensure the Left Panel mirrors this initial state perfectly. Ensure their slide animations use consistent widths and margins.
  </action>
  <verify>
    <automated>grep -A 10 "collapseLeftBtn.setOnAction" src/main/java/com/mapbuilder/mapbuilder/main/MainView.java</automated>
  </verify>
  <done>Left panel toggles smoothly and matches the layer panel state exactly on startup.</done>
</task>

<task type="auto">
  <name>Task 2: Theme Sliders to match dark UI</name>
  <files>src/main/resources/styles.css</files>
  <action>
    Add CSS rules for `.slider .track` and `.slider .thumb` to `styles.css` to match the `#3c3f41` and `#ffffff` dark UI theme of the application. The sliders should no longer use the default bright JavaFX styling.
  </action>
  <verify>
    <automated>grep ".slider .track" src/main/resources/styles.css</automated>
  </verify>
  <done>Sliders use dark themed colors for track and thumb.</done>
</task>

</tasks>

<success_criteria>
- Both collapsable panels exhibit the exact same open/close state logic and animations.
- JavaFX sliders look native to the dark theme.
</success_criteria>