---
phase: quick
plan: 260408-i22
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java
  - src/main/resources/styles.css
autonomous: true
requirements: [UI-TWEAKS]
must_haves:
  truths:
    - "Randomizing settings also randomizes hydrology features"
    - "Tab panes have no random shadow at the bottom"
  artifacts:
    - path: "src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java"
      provides: "Updated randomizeSettings method"
    - path: "src/main/resources/styles.css"
      provides: "CSS overrides for TabPane"
  key_links: []
---

<objective>
Fix the shadow at the bottom of the tab panes and make the randomize button also randomize the hydrology features.

Purpose: Improve UI polish and ensure the randomize button covers all relevant generator settings.
Output: Updated MainPresenter.java and styles.css.
</objective>

<context>
@.planning/STATE.md
@src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java
@src/main/resources/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Randomize Hydrology Features</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java</files>
  <action>
    Update the `randomizeSettings` method in `MainPresenter.java` to also set random values for the hydrology sliders:
    - `view.getRiverDensitySlider().setValue(rand.nextDouble(0, 200))`
    - `view.getLakeSizeSlider().setValue(rand.nextDouble(0, 200))`
    - `view.getMinLakeAreaSlider().setValue(rand.nextDouble(10, 500))`
  </action>
  <verify>
    <automated>grep -q "getRiverDensitySlider" src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java</automated>
  </verify>
  <done>Hydrology features are randomized when the button is clicked</done>
</task>

<task type="auto">
  <name>Task 2: Fix TabPane Shadow</name>
  <files>src/main/resources/styles.css</files>
  <action>
    Update `styles.css` to remove the default shadow/border at the bottom of the tab panes.
    Target `.tab-pane` and `.tab-pane .tab-content-area` to include:
    ```css
    -fx-effect: null;
    -fx-background-insets: 0;
    -fx-border-color: transparent;
    -fx-border-width: 0;
    ```
    Ensure no stray shadows appear near the tab content edge.
  </action>
  <verify>
    <automated>grep -q "\-fx-effect: null;" src/main/resources/styles.css</automated>
  </verify>
  <done>No shadow appears at the bottom of the tab panes</done>
</task>

</tasks>

<success_criteria>
Hydrology sliders are included in the randomizer and the tab panes look perfectly flat with no drop shadow.
</success_criteria>
