---
phase: quick
plan: 260408-hz5
type: execute
wave: 1
depends_on: []
files_modified: ["src/main/resources/styles.css"]
autonomous: true
requirements: ["UI-STYLE"]
must_haves:
  truths:
    - Checkboxes match the dark theme (no default blue/white artifacts).
    - Tabs have no blue focus artifacts or lines.
    - Tab panes don't have a random shadow at the bottom.
  artifacts:
    - path: src/main/resources/styles.css
      provides: Styling for Checkboxes, Tabs, and Tab panes.
  key_links: []
---

<objective>
Update `styles.css` to ensure checkboxes match the dark theme, remove blue artifacts from tabs, and fix the random shadow at the bottom of the tab panes.
</objective>

<context>
@src/main/resources/styles.css
</context>

<tasks>
<task type="auto">
  <name>Task 1: Style Checkboxes, Tabs, and Tab Panes</name>
  <files>src/main/resources/styles.css</files>
  <action>
    Add and modify CSS rules in `src/main/resources/styles.css` to:
    1. Style the `.check-box` to match the dark theme (e.g. background `#3c3f41`, mark `#ffffff`, box `#555555`). Remove default JavaFX blue colors.
    2. Remove the blue artifact from tabs by styling `.tab-pane:focused .tab-header-area .tab-header-background` or `.tab-pane .tab:selected` (add `-fx-focus-color: transparent; -fx-faint-focus-color: transparent;`).
    3. Fix the random shadow at the bottom of the tab panes (often caused by the default `tab-content-area` border or padding). Set `-fx-padding: 0;` and remove border/background artifacts on `.tab-pane .tab-content-area`.
  </action>
  <verify>
    <automated>grep -q ".check-box" src/main/resources/styles.css &amp;&amp; grep -q "\-fx-focus-color: transparent" src/main/resources/styles.css</automated>
  </verify>
  <done>Checkboxes are dark-themed, tabs lack blue artifacts, and the tab pane shadow is gone.</done>
</task>
</tasks>

<success_criteria>
Checkboxes correctly blend with the app's dark theme, tabs do not show blue selections, and there is no bottom shadow on the tab content.
</success_criteria>
