---
phase: quick
plan: j0l
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/styles.css
  - src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - Text contrast is fixed for the dark theme panels
    - Collapse icons are unified
    - Controls (sliders, buttons, textfields) fit the dark theme
  artifacts:
    - path: src/main/resources/styles.css
      provides: Dark theme styles
    - path: src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
      provides: Unified UI components
  key_links: []
---

<objective>
Modify the UI to fix contrast issues, unify the collapse icons, and update control styles to fit the dark theme.

Purpose: Improve readability and visual consistency across the application.
Output: Updated CSS and JavaFX view code.
</objective>

<context>
@src/main/resources/styles.css
@src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS for dark theme and contrast</name>
  <files>src/main/resources/styles.css</files>
  <action>
    Fix the contrast issues by changing black text and tick labels to white or light gray.
    Currently, `.left-panel .label` has `-fx-text-fill: #000000` on a dark background `#2b2b2b`. Change text fills to `#ffffff` or `#dddddd`.
    Update the slider ticks (`.slider .axis`, `:minor-tick`, `:major-tick`) to have light strokes and labels.
    Add standard styling for controls like text fields, buttons, and tab panes to make them look cohesive with the dark theme.
  </action>
  <verify>
    <automated>./gradlew classes</automated>
  </verify>
  <done>CSS updated with high-contrast text and cohesive dark theme control styles.</done>
</task>

<task type="auto">
  <name>Task 2: Unify collapse icons and apply styles in MainView</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainView.java</files>
  <action>
    Unify the collapse icons used for `collapseLeftBtn` and `collapseRightBtn` (e.g., use `"<"` and `">"` or `"\u25C0"` and `"\u25B6"`).
    Ensure the right and left panel header buttons are consistent.
    Ensure any needed CSS classes are applied to the controls (buttons, text fields, sliders) if they need specific styling from the CSS file.
  </action>
  <verify>
    <automated>./gradlew classes</automated>
  </verify>
  <done>Collapse icons are unified and MainView applies correct styling classes.</done>
</task>

</tasks>

<verification>
./gradlew classes
</verification>

<success_criteria>
- Text in the left and right panels is legible against the dark background.
- Left and right collapse buttons use matching icon conventions.
- Form controls (sliders, fields) match the overall dark aesthetic.
</success_criteria>

<output>
After completion, create `.planning/quick/260407-j0l-modify-the-ui-only-now-to-fix-the-contra/j0l-SUMMARY.md`
</output>