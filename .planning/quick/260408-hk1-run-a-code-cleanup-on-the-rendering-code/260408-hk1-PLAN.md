---
phase: quick
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/mapbuilder/mapbuilder/core/MVPBase.java
  - src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
  - src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java
autonomous: true
requirements: [REF-01]
must_haves:
  truths:
    - MVPBase interface is completely removed
    - MainView UI construction is split into logical private methods
  artifacts:
    - path: src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
      provides: "Refactored, modular view creation"
      min_lines: 200
  key_links: []
---

<objective>
Refactor MainView to improve code readability by breaking the large constructor into smaller private setup methods, and remove the unnecessary MVPBase interface to simplify the architecture.

Purpose: Code cleanup and maintainability, removing useless abstractions.
Output: Cleaned up MainView.java and MainPresenter.java, deleted MVPBase.java.
</objective>

<context>
@src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
@src/main/java/com/mapbuilder/mapbuilder/core/MVPBase.java
@src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove MVPBase interface</name>
  <files>
    src/main/java/com/mapbuilder/mapbuilder/core/MVPBase.java
    src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
    src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java
  </files>
  <action>
    - Delete `src/main/java/com/mapbuilder/mapbuilder/core/MVPBase.java`.
    - In `MainView.java`, remove `implements MVPBase.View` and the `@Override` annotations on all the getter methods. Remove the import for `MVPBase`.
    - In `MainPresenter.java`, remove `implements MVPBase.Presenter<MainView>`, the `@Override` annotations for `setView` and `getView`, and the import for `MVPBase`.
  </action>
  <verify>
    <automated>! grep -rnw 'src' -e 'MVPBase'</automated>
  </verify>
  <done>MVPBase interface is fully removed from the codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor MainView constructor</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainView.java</files>
  <action>
    Extract UI initialization from the `MainView` constructor into logically grouped private methods:
    - `setupCanvasContainer()`: Initialize canvas, canvasGroup, scrolling, and panning.
    - `setupLeftPanel()`: Initialize the left panel, tabs, sliders, generator buttons, and animations.
    - `setupTopActionBar()`: Initialize the Save/Load/Export/Print buttons.
    - `setupRightLayersPanel()`: Initialize the layers list, toggle buttons, and collapse animations.
    Call these methods from the constructor and add the resulting components to `this.getChildren()`.
    Ensure all UI references (like canvas, sliders, buttons) are correctly assigned to the class fields.
    Do NOT modify the map generation or any map logic.
  </action>
  <verify>
    <automated>./gradlew classes</automated>
  </verify>
  <done>MainView's constructor is short and delegates to private setup methods; project compiles.</done>
</task>

</tasks>

<verification>
- View class refactored and working.
- No MVPBase references remaining.
- Code cleanly compiles.
</verification>

<success_criteria>
- MVPBase.java is removed.
- MainView and MainPresenter no longer depend on MVPBase.
- MainView constructor is modularized into several private setup methods.
- The project compiles correctly with `./gradlew classes`.
</success_criteria>

<output>
After completion, create `.planning/phases/quick/quick-1-SUMMARY.md`
</output>
