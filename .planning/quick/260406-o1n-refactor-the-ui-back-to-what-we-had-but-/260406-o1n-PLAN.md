---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
  - src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java
autonomous: true
requirements:
  - QUICK-01
must_haves:
  truths:
    - "UI layout is restored to previous desired state"
    - "Map Editor and generation tools remain fully functional"
    - "Map can be zoomed in and out via scroll"
  artifacts:
    - path: "src/main/java/com/mapbuilder/mapbuilder/main/MainView.java"
      provides: "Restored UI and zoom functionality"
  key_links: []
---

<objective>
Refactor the UI back to what we had, ensuring we keep the map editor component, and make the map canvas zoomable.

Purpose: Regain the old layout while retaining the map generator and adding necessary zoom capabilities for large map navigation.
Output: Restored layout with a zoomable canvas.
</objective>

<context>
@src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
@src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore UI Layout & Preserve Map Editor</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainView.java, src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java</files>
  <action>
    Analyze the recent git history or provided sketch to understand "what we had" regarding the UI layout, and restore that structure.
    Crucially, extract and retain the existing map canvas and its associated generator controls (seed, size, octaves, etc.), integrating them into the restored layout.
  </action>
  <verify>
    <automated>./gradlew classes</automated>
  </verify>
  <done>UI layout matches previous state without dropping map generation controls.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Map Zoom Interaction</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainView.java, src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java</files>
  <action>
    Add zoom capabilities to the Map Editor canvas.
    - Attach scroll event listeners to the canvas or its wrapper.
    - Adjust `scaleX` and `scaleY` properties based on scroll wheel delta to zoom in/out.
    - Restrict scaling with appropriate min/max limits (e.g., 0.1 to 10.0).
  </action>
  <verify>
    <automated>./gradlew build -x test</automated>
  </verify>
  <done>Scroll wheel vertically zooms the map canvas in and out within boundaries.</done>
</task>

</tasks>

<verification>
Ensure the project builds cleanly. Run the application to manually verify that the map is zoomable and the layout is properly restored.
</verification>

<success_criteria>
- The UI structure is reverted to the specified previous state.
- Map Editor features (canvas and controls) remain accessible and functional.
- The map can be zoomed in and out via scrolling.
</success_criteria>

<output>
After completion, report success.
</output>