---
phase: 03-interactive-elements-pois-labels
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java
  - src/main/resources/assets/poi-icons.png
  - src/main/java/com/mapbuilder/mapbuilder/ui/POIIconMapper.java
autonomous: false
requirements: []
must_haves:
  truths:
    - POI overlay canvas renders above main map canvas in StackPane (D-08)
    - Each POI renders as colored circle (12–16px) + sprite icon (16x16px) (D-09)
    - POI labels appear on mouse hover only (D-11)
    - Layer toggle controls POI visibility (D-12)
    - POI overlay canvas separate from biome/kingdom canvas (prevents flicker)
    - Click events on POI canvas work (selection for editing)
  artifacts:
    - path: src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java
      provides: renderPOIs() method called after renderMap()
      contains: "renderPOIs"
    - path: src/main/resources/assets/poi-icons.png
      provides: 256x256px sprite sheet with 16 icons (16x16 each)
      min_bytes: 5000
    - path: src/main/java/com/mapbuilder/mapbuilder/ui/POIIconMapper.java
      provides: Index mapping of POI types to sprite sheet coordinates
      contains: "POIType.*=" 
  key_links:
    - from: MainPresenter.renderMap()
      to: MainPresenter.renderPOIs()
      via: Sequential call after map render complete
      pattern: "renderMap\\(\\).*renderPOIs\\(\\)"
    - from: MainPresenter.renderPOIs()
      to: POIIconMapper
      via: Look up sprite index for POI type
      pattern: "POIIconMapper\\."
    - from: MainView
      to: POI canvas
      via: StackPane integration for overlay
      pattern: "poiCanvas"
---

<objective>
**Phase 3.3: POI Visual Rendering & Layer Integration**

Implement POI rendering as a separate overlay canvas with colored circles + sprite icons (D-08 to D-12). Integrate POI visibility toggle into layer panel. Enable hover labels for clarity.

**Purpose:** Make generated POIs visible on the map with a clean, performant rendering approach. Separate POI canvas decouples from biome/kingdom updates (no flicker on slider changes).

**Output:**
- MainPresenter.renderPOIs() method
- POIIconMapper.java for sprite sheet indexing
- poi-icons.png sprite asset
- POI canvas integrated into MainView StackPane
- Hover label rendering
- Checkpoint for asset approval
</objective>

<execution_context>
@.planning/phases/03-interactive-elements-pois-labels/03-CONTEXT.md
@.planning/phases/03-interactive-elements-pois-labels/03-01-PLAN.md
@.planning/phases/03-interactive-elements-pois-labels/03-02-PLAN.md
</execution_context>

<context>
Phase 3 context specifies:
- **D-08:** Separate JavaFX Canvas for POI rendering, overlaid above map canvas via StackPane
- **D-09:** Colored circle (12–16px diameter) + sprite icon (16x16px) centered on circle
- **D-10:** Sprite sheet PNG/SVG (256x256px, 16 icons at 16x16 each tightly packed)
- **D-11:** Labels appear on hover only, white/black shadow for legibility, ~11px font
- **D-12:** Layer panel toggle controls POI canvas opacity (0 or 1), data persists

MainView already uses StackPane and GraphicsContext; MainPresenter.renderMap() completes at ~line 150. Integration point: add renderPOIs() call immediately after renderMap().

Icon mapping reference: Each POI type (CITY, DUNGEON, LANDMARK, etc.) maps to a sprite sheet index. E.g., CITY=0 (0,0), DUNGEON=1 (16,0), LANDMARK=2 (32,0), etc.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>POI sprite sheet asset (poi-icons.png or poi-icons.svg)</what-built>
  <how-to-verify>
    Before proceeding with rendering code, verify the sprite asset exists and is correct:
    
    1. Run: `ls -lh src/main/resources/assets/poi-icons.*`
    2. Expected: 256x256px PNG or SVG file > 5KB
    3. Visual inspection: Open poi-icons.png in an image viewer and confirm:
       - At least 16 distinct icons (CITY=building, DUNGEON=skull, LANDMARK=peak, RUIN=broken-building, VILLAGE=house, CASTLE=fort, TOWER=tower, CAVE=cave, SHRINE=altar, TAVERN=mug, etc.)
       - Each icon is 16x16px
       - Icons are tightly packed in a grid (0,0 to 240,240 with 16px spacing)
       - No transparency issues; icons are clear and legible at 16x16
       
    If asset already exists from prior work, skip and type "approved". If not, pause here.
  </how-to-verify>
  <resume-signal>Type "approved" once poi-icons.png is confirmed, or provide path to existing sprite asset</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Create POIIconMapper for sprite sheet indexing</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/ui/POIIconMapper.java</files>
  <read_first>
    src/main/java/com/mapbuilder/mapbuilder/core/map/POIType.java (all type values)
  </read_first>
  <action>
    Create POIIconMapper.java as a utility class with a static method:

    `public static int[] getSpriteCoordinates(POIType type)` returning [x, y] pixel coordinates in the sprite sheet for that type.

    Define a mapping from POIType to sprite sheet index:
    - CITY → (0, 0)
    - DUNGEON → (16, 0)
    - LANDMARK → (32, 0)
    - RUIN → (48, 0)
    - VILLAGE → (64, 0)
    - CASTLE → (80, 0)
    - TOWER → (96, 0)
    - CAVE → (112, 0)
    - SHRINE → (128, 0)
    - TAVERN → (144, 0)
    (Add more if POIType has additional types)

    Also provide:
    `public static int[] getDefaultColor(POIType type)` returning ARGB color (e.g., 0xFF4CAF50 for green = CITY).

    Define a color palette:
    - CITY → 0xFF4CAF50 (green)
    - DUNGEON → 0xFF3F51B5 (dark blue)
    - LANDMARK → 0xFFFF9800 (orange)
    - RUIN → 0xFF795548 (brown)
    - VILLAGE → 0xFFFFC107 (amber)
    - CASTLE → 0xFF9C27B0 (purple)
    - TOWER → 0xFF607D8B (blue-grey)
    - CAVE → 0xFF424242 (dark grey)
    - SHRINE → 0xFFE91E63 (pink)
    - TAVERN → 0xFF8B4513 (saddle-brown)

    These colors provide visual distinction on hover or in UI list.
  </action>
  <verify>
    <automated>grep -c "getSpriteCoordinates\|getDefaultColor" src/main/java/com/mapbuilder/mapbuilder/ui/POIIconMapper.java</automated>
    <automated>grep -E "CITY|DUNGEON|LANDMARK|RUIN|VILLAGE|CASTLE|TOWER|CAVE|SHRINE|TAVERN" src/main/java/com/mapbuilder/mapbuilder/ui/POIIconMapper.java | wc -l | grep -E "[1-9][0-9]"</automated>
    <automated>mvn compile 2>&1 | grep -i "error" | wc -l | grep "^0$"</automated>
  </verify>
  <done>POIIconMapper.java created with getSpriteCoordinates() and getDefaultColor(); all POI types mapped; file compiles</done>
</task>

<task type="auto">
  <name>Task 2: Implement renderPOIs() method in MainPresenter</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java</files>
  <read_first>
    src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java (renderMap() method and zoom/pan logic)
    src/main/java/com/mapbuilder/mapbuilder/main/MainModel.java (getMapGrid() to access POIs)
    src/main/java/com/mapbuilder/mapbuilder/ui/POIIconMapper.java (sprite and color mapping)
  </read_first>
  <action>
    Add new method `private void renderPOIs()` to MainPresenter:

    1. Get POI list from MainModel: `List<PointOfInterest> pois = model.getMapGrid().getPointsOfInterest();`

    2. Get POI canvas from MainView (create accessor if needed): `Canvas poiCanvas = view.getPoiCanvas();`

    3. Get GraphicsContext: `GraphicsContext gc = poiCanvas.getGraphicsContext2D();`

    4. Clear canvas: `gc.clearRect(0, 0, poiCanvas.getWidth(), poiCanvas.getHeight());`

    5. For each POI in the list:
       a. Calculate screen coordinates from map coordinates (apply zoom and viewport centering, same transformation as biome/kingdom rendering)
       b. Get default color from POIIconMapper.getDefaultColor(poi.getType()); use customColor if not null
       c. Draw filled circle (D-09): `gc.setFill(color); gc.fillOval(screenX - 6, screenY - 6, 12, 12);` (center at screenX, screenY, diameter 12px)
       d. Draw sprite icon: Load sprite sheet, extract 16x16 region using POIIconMapper.getSpriteCoordinates(), draw centered on circle using `gc.drawImage(spriteSheet, srcX, srcY, 16, 16, screenX - 8, screenY - 8, 16, 16);`

    6. Call this method immediately after renderMap() in the render() method or wherever map rendering happens.

    For hover labels (D-11): Track mouse position in MainView (existing mouse tracking). In renderPOIs(), check if any POI is within 20px of mouse position. If yes, render label above that POI:
       - Font: ~11px
       - Text: POI name
       - Shadow: black or white outline for legibility
       - Position: Above circle (screenY - 20)

    Use cached sprite image to avoid reloading on each render.
  </action>
  <verify>
    <automated>grep -c "private void renderPOIs" src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java</automated>
    <automated>grep "renderPOIs()" src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java | wc -l | grep -E "[1-9]"</automated>
    <automated>grep "fillOval\|drawImage" src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java | wc -l | grep -E "[2-9]"</automated>
    <automated>mvn compile 2>&1 | grep -i "error" | wc -l | grep "^0$"</automated>
  </verify>
  <done>renderPOIs() method added to MainPresenter; circles and sprite icons drawn per D-09; hover labels implemented; called after renderMap(); file compiles</done>
</task>

<task type="auto">
  <name>Task 3: Integrate POI canvas into MainView StackPane and wire layer toggle</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainView.java</files>
  <read_first>
    src/main/java/com/mapbuilder/mapbuilder/main/MainView.java (setupRightLayersPanel() at line 357, current StackPane structure)
    src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java (layer toggle listeners)
  </read_first>
  <action>
    1. Add POI canvas to MainView:
       - Create new Canvas: `this.poiCanvas = new Canvas(mapCanvas.getWidth(), mapCanvas.getHeight());`
       - Add to StackPane above mapCanvas: `canvasContainer.getChildren().add(poiCanvas);` (add after mapCanvas so it renders on top)
       - Set mouse-transparent false initially so click events work: `poiCanvas.setMouseTransparent(false);`
       - Provide getter: `public Canvas getPoiCanvas() { return poiCanvas; }`

    2. In setupRightLayersPanel():
       - Find existing layer toggles (Terrain, Kingdoms, etc.)
       - Add new toggle for "Punkte von Interesse" (POIs) using same style/pattern as existing toggles
       - Wire to listener that controls POI canvas opacity:
         ```
         poiToggle.selectedProperty().addListener((obs, oldVal, newVal) -> {
           poiCanvas.setOpacity(newVal ? 1.0 : 0.0);
         });
         ```
       - Initialize toggle to true (POIs visible by default)

    3. Wire click events (optional, for future Phase 3.4):
       - poiCanvas can capture clicks to select POIs for editing
       - For now, bubble clicks through to mapCanvas for zoom/pan (set canvas.setMouseTransparent(false) but don't consume events if click misses POI)

    4. On MainPresenter.render() completion, call view.getPoiCanvas().setVisible(true) to ensure canvas is ready.
  </action>
  <verify>
    <automated>grep -c "poiCanvas\|getPoiCanvas" src/main/java/com/mapbuilder/mapbuilder/main/MainView.java | grep -E "[2-9]"</automated>
    <automated>grep "Punkte von Interesse" src/main/java/com/mapbuilder/mapbuilder/main/MainView.java</automated>
    <automated>grep "setOpacity" src/main/java/com/mapbuilder/mapbuilder/main/MainView.java</automated>
    <automated>mvn compile 2>&1 | grep -i "error" | wc -l | grep "^0$"</automated>
  </verify>
  <done>POI canvas created and added to StackPane; layer toggle wired to opacity control; getter provided for MainPresenter; file compiles</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>POI rendering on main canvas with correct colors, icons, and hover labels</what-built>
  <how-to-verify>
    Run the application and verify:
    
    1. Map generates with POIs visible (colored circles with icons)
    2. POI layer toggle in right panel controls visibility (toggle on/off)
    3. Hovering over POI shows name label (e.g., "Ashland City")
    4. Colors match POI types (CITY=green, DUNGEON=blue, etc.)
    5. Icons from sprite sheet render centered on circles
    6. Zoom/pan works with POI canvas overlay (no blocking interactions yet)
    7. No rendering artifacts or flickering when toggling layer or adjusting sliders
    
    Expected observation: 12–16px circles with 16x16 sprite icons, labels appear on hover, separate canvas prevents flicker.
    
    If any issue appears, describe it and we'll fix in revision loop.
  </how-to-verify>
  <resume-signal>Type "approved" if rendering looks correct, or describe any visual issues (e.g., "icons not centered", "labels not showing", "wrong colors")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sprite sheet loading | Asset file verified once at startup; no dynamic reloading |
| Canvas coordinates | Screen position calculated from zoom/pan state; validation in rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Denial of Service | Large POI count rendering | mitigate | Rendering O(n) in POI count; canvas redraw per frame is acceptable for typical counts (<500 POIs) |
| T-03-09 | Repudiation | Sprite asset version | accept | Sprite sheet is static; no versioning needed (Phase 5 save/load doesn't depend on visual assets) |
</threat_model>

<verification>
After all tasks complete:
1. Verify renderPOIs() exists and is called: `grep -A1 "renderMap()" src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java | grep "renderPOIs"`
2. Verify POI canvas in MainView: `grep "poiCanvas" src/main/java/com/mapbuilder/mapbuilder/main/MainView.java | wc -l` (should be > 5)
3. Verify layer toggle wired: `grep "Punkte von Interesse" src/main/java/com/mapbuilder/mapbuilder/main/MainView.java`
4. Compile: `mvn compile` (no errors)
5. Visual test: Run application, generate map, verify POIs render
</verification>

<success_criteria>
- [ ] POI canvas created and added to StackPane
- [ ] renderPOIs() method implemented in MainPresenter
- [ ] Colored circles + sprite icons rendered for each POI (D-09)
- [ ] Hover labels appear with POI names (D-11)
- [ ] Layer toggle controls visibility (D-12)
- [ ] POI canvas separate from map canvas (no flicker on slider changes)
- [ ] All files compile without errors
- [ ] Visual checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-elements-pois-labels/03-03-SUMMARY.md` with:
- POI rendering complete (circles + icons + labels)
- Layer integration done
- Ready for Plan 4: POI editing UI (modal dialog)
</output>
