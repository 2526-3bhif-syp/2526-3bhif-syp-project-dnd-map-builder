---
phase: 03-interactive-elements-pois-labels
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java
  - src/main/java/com/mapbuilder/mapbuilder/core/map/PointOfInterestGenerator.java
  - src/test/java/com/mapbuilder/mapbuilder/core/map/PointOfInterestGeneratorTest.java
autonomous: true
requirements: []
must_haves:
  truths:
    - POI generation happens after kingdom generation completes
    - Kingdom cities are placed at capital cells (D-03)
    - Dungeons placed at multi-kingdom borders and high-elevation caves (D-04)
    - Landmarks placed at waterfalls and mountain peaks (D-05)
    - Settlements scattered with Poisson-disc sampling (D-06)
    - Three density sliders control POI counts (D-07)
  artifacts:
    - path: src/main/java/com/mapbuilder/mapbuilder/core/map/PointOfInterestGenerator.java
      provides: POI generation algorithm implementing D-03 to D-07
      min_lines: 200
    - path: src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java
      provides: Updated generate() call sequence
      contains: "generatePointsOfInterest"
    - path: src/test/java/com/mapbuilder/mapbuilder/core/map/PointOfInterestGeneratorTest.java
      provides: Unit tests for each generation rule
      contains: "test.*Generate.*POI"
  key_links:
    - from: MapGenerator.generate()
      to: PointOfInterestGenerator
      via: Call to generatePointsOfInterest() after kingdom generation
      pattern: "generatePointsOfInterest\\(grid"
    - from: PointOfInterestGenerator
      to: MapGrid.setPointsOfInterest()
      via: Stores generated POI list
      pattern: "grid.setPointsOfInterest"
---

<objective>
**Phase 3.2: POI Auto-Generation Rules Engine**

Implement world-building rules for automatic POI placement: kingdom cities (D-03), border dungeons (D-04), terrain landmarks (D-05), scattered settlements (D-06), and user-controlled density parameters (D-07).

**Purpose:** Create the generation logic that produces diverse, meaningful POIs based on map topology. Enables the POI density sliders to trigger regeneration and maintain the same rendering pipeline as Phase 2.

**Output:**
- PointOfInterestGenerator.java (standalone utility class)
- Extended MapGenerator.generate() call sequence
- Unit tests for each generation rule
</objective>

<execution_context>
@.planning/phases/03-interactive-elements-pois-labels/03-CONTEXT.md
@.planning/phases/03-interactive-elements-pois-labels/03-01-PLAN.md (prior plan created PointOfInterest model)
</execution_context>

<context>
Phase 3 context defines four generation rules (D-03 to D-06) and user-controlled density parameters (D-07). POI generation must integrate into MapGenerator.generate() AFTER kingdom generation completes (D-18: rendering pipeline). The generation must be synchronous (no separate async phase — D-20).

Key topology references:
- **D-03:** Kingdom capitals already identified by MapGenerator (each Kingdom has a capital cell)
- **D-04:** Multi-kingdom borders (cell where 3+ Kingdom pointers exist), cave/mountain clusters (high elevation + low kingdom probability)
- **D-05:** Waterfalls (high elevation adjacent to water), mountain peaks (highest elevation per kingdom cluster)
- **D-06:** Biome-based settlement probability, Poisson-disc sampling for spacing

User parameters passed from UI (will be wired in Plan 3):
- `dungeonDensity` (0.0–1.0, default 0.5)
- `landmarkDensity` (0.0–1.0, default 0.3)
- `settlementDensity` (0.0–1.0, default 0.4)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PointOfInterestGenerator utility class</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/core/map/PointOfInterestGenerator.java</files>
  <read_first>
    src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java (reference generation pattern and kingdom/biome access)
    src/main/java/com/mapbuilder/mapbuilder/core/map/MapCell.java (elevation, biome, kingdom, river/lake fields)
    src/main/java/com/mapbuilder/mapbuilder/core/map/Kingdom.java (capital field access)
    src/main/java/com/mapbuilder/mapbuilder/core/map/Biome.java (biome types for settlement logic)
  </read_first>
  <action>
    Create PointOfInterestGenerator as a public utility class with a single static method:
    
    `public static List<PointOfInterest> generatePointsOfInterest(MapGrid grid, int seed, double dungeonDensity, double landmarkDensity, double settlementDensity)`

    The method returns a new List<PointOfInterest> (not modified in place; caller assigns via grid.setPointsOfInterest()).

    Implement four sub-methods (private static):
    1. `addKingdomCities(List<PointOfInterest>, MapGrid, int seed)` — D-03: Place CITY at each kingdom's capital. Iterate grid.getKingdoms() (or find unique kingdoms from cells), place CITY POI at capital with name "{KingdomName} City" and createdByRule="kingdom_capital".

    2. `addDungeonAndRuins(List<PointOfInterest>, MapGrid, int seed, double dungeonDensity)` — D-04: Place DUNGEON/RUIN at multi-kingdom borders and high caves. Count = (int)(grid.getWidth() * grid.getHeight() * (dungeonDensity / 100.0) * 0.15). For each placement: check if cell is on 3+ kingdom border OR (elevation > waterLevel+0.2 AND biome is cave/mountain AND kingdom probability < 0.1). Use pseudo-random cell selection (Random seeded) to deterministically pick locations.

    3. `addLandmarks(List<PointOfInterest>, MapGrid, int seed, double landmarkDensity)` — D-05: Place LANDMARK at waterfalls and peaks. Count = (int)(grid.getWidth() * grid.getHeight() * (landmarkDensity / 100.0) * 0.1). For each: check if cell is high elevation (> waterLevel+0.3) adjacent to water (rivers/coasts), or is highest elevation in a kingdom cluster. Create LANDMARK POI with name "Peak of {Kingdom}" or "Waterfall Falls".

    4. `addSettlements(List<PointOfInterest>, MapGrid, int seed, double settlementDensity)` — D-06: Scatter VILLAGE/TOWN POIs using Poisson-disc sampling. Target count = (int)(grid.getWidth() * grid.getHeight() * (settlementDensity / 100.0) * 0.25). Filter to habitable biomes (grassland, temperate forest, savanna — see Biome enum). Use simple greedy Poisson-disc with minimum distance 5-10 cells. For each placement, create VILLAGE POI with name "Village of {RandomName}" and createdByRule="settlement_scattered".

    Naming helpers (private static methods):
    - `String generateCityName(int kingdomId, int seed)` — Use kingdom ID and seed to deterministically generate names like "Ashland City", "Westmarch City", etc.
    - `String generateVillageName(int seed)` — Generate generic fantasy names like "Briarwood", "Sunhaven", etc.

    Call order in generatePointsOfInterest():
    1. addKingdomCities (no density scaling)
    2. addDungeonAndRuins (scaled by dungeonDensity)
    3. addLandmarks (scaled by landmarkDensity)
    4. addSettlements (scaled by settlementDensity)

    Ensure POI ids are unique and sequential (0, 1, 2, ...) across all adds. Use an AtomicInteger or pass a counter as reference.
  </action>
  <verify>
    <automated>grep -c "public static List<PointOfInterest> generatePointsOfInterest" src/main/java/com/mapbuilder/mapbuilder/core/map/PointOfInterestGenerator.java</automated>
    <automated>grep -c "addKingdomCities\|addDungeonAndRuins\|addLandmarks\|addSettlements" src/main/java/com/mapbuilder/mapbuilder/core/map/PointOfInterestGenerator.java</automated>
    <automated>mvn compile 2>&1 | grep -i "error" | wc -l | grep "^0$"</automated>
  </verify>
  <done>PointOfInterestGenerator.java created with main method and 4 sub-methods; file compiles without errors; naming helpers present</done>
</task>

<task type="auto">
  <name>Task 2: Integrate POI generation into MapGenerator.generate() call sequence</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java</files>
  <read_first>
    src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java (current generate() method)
    src/main/java/com/mapbuilder/mapbuilder/core/map/PointOfInterestGenerator.java (new generator class)
  </read_first>
  <action>
    Update MapGenerator.generate() signature to accept three new parameters (add at end of parameter list):
    - `double dungeonDensity` (default 0.5)
    - `double landmarkDensity` (default 0.3)
    - `double settlementDensity` (default 0.4)

    Update the method body:
    1. After `generateKingdoms()` call (line ~122), add:
       ```
       List<PointOfInterest> pois = PointOfInterestGenerator.generatePointsOfInterest(
         grid, seed, dungeonDensity, landmarkDensity, settlementDensity
       );
       grid.setPointsOfInterest(pois);
       ```

    2. Update the Javadoc to document the new parameters.

    This ensures POI generation runs synchronously after kingdoms, before returning from generate(). No async threads needed (D-20: synchronous).

    Do NOT change existing tests yet — that's verified by existing MapGeneratorTest.
  </action>
  <verify>
    <automated>grep -c "dungeonDensity\|landmarkDensity\|settlementDensity" src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java</automated>
    <automated>grep "grid.setPointsOfInterest" src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java | wc -l | grep -E "[1-9]"</automated>
    <automated>mvn compile 2>&1 | grep -i "error" | wc -l | grep "^0$"</automated>
  </verify>
  <done>MapGenerator.generate() updated with 3 new density parameters; PointOfInterestGenerator.generatePointsOfInterest() called after kingdom generation; file compiles</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for POI generation rules</name>
  <files>
    src/test/java/com/mapbuilder/mapbuilder/core/map/PointOfInterestGeneratorTest.java
    src/main/java/com/mapbuilder/mapbuilder/core/map/PointOfInterestGenerator.java
  </files>
  <read_first>
    src/test/java/com/mapbuilder/mapbuilder/core/map/MapGeneratorTest.java (reference test structure)
  </read_first>
  <behavior>
    Test 1: Kingdom cities generated — one CITY POI per kingdom at capital cell
    Test 2: Kingdom city names are unique and deterministic (same seed = same names)
    Test 3: Dungeons generated at multi-kingdom border cells (3+ adjacent kingdoms)
    Test 4: Dungeons count scales with dungeonDensity (0.0 → 0 dungeons, 1.0 → max count)
    Test 5: Landmarks generated at high-elevation peaks
    Test 6: Landmarks placed adjacent to water (rivers or coasts)
    Test 7: Settlements use Poisson-disc sampling (minimum distance maintained between villages)
    Test 8: Settlement count scales with settlementDensity
    Test 9: All generated POIs have unique sequential ids (0, 1, 2, ...)
    Test 10: Generated list contains no duplicates (POI ids unique)
    Test 11: Density = 0.0 produces 0 POIs of that type (dungeon, landmark, settlement)
    Test 12: Density = 1.0 produces maximum POIs of that type
    Test 13: Kingdom cities always generated regardless of dungeonDensity (independent)
    Test 14: Generated POIs reference valid terrain (x/y within grid bounds, biome is not null)
  </behavior>
  <action>
    Create PointOfInterestGeneratorTest.java with test methods covering all behaviors. Set up a test MapGrid with known kingdoms, cells, and terrain (use MapGenerator with fixed seed to create predictable state). Call generatePointsOfInterest() with various density settings and verify:
    - Correct POI types appear
    - Counts match density expectations
    - Spatial rules are respected (borders, water, elevation, spacing)
    - ids are sequential and unique
    - All POIs are within bounds

    Use MapGeneratorTest structure as reference (setUp, helper methods, assertions).
  </action>
  <verify>
    <automated>mvn test -Dtest=PointOfInterestGeneratorTest 2>&1 | grep -i "tests run\|failures\|errors" | tail -1</automated>
  </verify>
  <done>PointOfInterestGeneratorTest.java created with 14+ test methods; all tests pass (0 failures); code covers all generation rules and density scaling</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Density parameters | User-supplied 0.0–1.0 values; clamped in generation logic before use |
| Random seed | Deterministic generation enabled via seeded Random; reproducible maps from same seed |
| POI placement | Generated within grid bounds; validation in PointOfInterestGenerator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Density parameters | mitigate | Clamp values to 0.0–1.0 before use in formulas; prevent negative counts |
| T-03-06 | Denial of Service | POI count explosion | mitigate | Maximum POI count capped by grid area * max density (1.0) = predictable ceiling |
| T-03-07 | Information Disclosure | POI placement algorithm | accept | Deterministic seeding means maps are reproducible; no randomness leaks secrets |
</threat_model>

<verification>
After all tasks complete:
1. Verify PointOfInterestGenerator compiles: `mvn compile`
2. Verify MapGenerator.generate() signature includes 3 density parameters: `grep "double dungeonDensity" src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java`
3. Verify POI generation called after kingdoms: `grep -A2 "generateKingdoms" src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java | grep -c "setPointsOfInterest"`
4. Run tests: `mvn test -Dtest=PointOfInterestGeneratorTest` (all pass)
5. Run full test suite: `mvn test` (no regressions in existing MapGeneratorTest)
</verification>

<success_criteria>
- [ ] PointOfInterestGenerator.java created with main method and 4 sub-methods
- [ ] Each generation rule (D-03 to D-06) implemented
- [ ] Density parameters integrated into MapGenerator.generate()
- [ ] POI list cleared and regenerated on each generate() call
- [ ] PointOfInterestGeneratorTest.java covers all rules
- [ ] All tests pass (PointOfInterestGeneratorTest + existing MapGeneratorTest)
- [ ] Code compiles without errors
- [ ] POI ids sequential and unique across all generation rules
</success_criteria>

<output>
After completion, update `.planning/phases/03-interactive-elements-pois-labels/03-01-SUMMARY.md` to include POI generation tests, then create `.planning/phases/03-interactive-elements-pois-labels/03-02-SUMMARY.md` with:
- POI generation rules implemented (D-03 to D-07)
- MapGenerator extended with density parameters
- Tests confirm rule logic and density scaling
- Ready for Plan 3: Rendering & UI integration
</output>
