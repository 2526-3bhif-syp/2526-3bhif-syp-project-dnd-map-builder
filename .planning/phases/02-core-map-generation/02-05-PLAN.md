---
phase: 02-core-map-generation
plan: 05
type: execute
wave: 1
depends_on: ["02-01", "02-02"]
files_modified: ["src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java", "src/main/java/com/mapbuilder/mapbuilder/main/MainView.java", "src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java", "src/main/java/com/mapbuilder/mapbuilder/main/MainModel.java", "src/main/java/com/mapbuilder/mapbuilder/core/map/Kingdom.java"]
autonomous: true
requirements: ["MAP-01", "MAP-02"]
must_haves:
  truths:
    - Kingdom capitals are generated using Poisson Disc sampling
    - Voronoi cells expand using terrain cost (mountains/oceans are expensive)
    - Expansion stops exactly at the coastline (oceans are unowned)
    - Lloyd's relaxation smooths the borders based on user config
    - UI contains sliders for Kingdom Count and Relaxation Passes, and toggles for borders/overlays
  artifacts:
    - path: "src/main/java/com/mapbuilder/mapbuilder/core/map/Kingdom.java"
      provides: "Data model for a generated Kingdom"
    - path: "src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java"
      provides: "Poisson Disc sampling and cost-based Voronoi logic"
    - path: "src/main/java/com/mapbuilder/mapbuilder/main/MainView.java"
      provides: "Kingdom configuration UI controls"
  key_links:
    - from: "src/main/java/com/mapbuilder/mapbuilder/main/MainView.java"
      to: "src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java"
      via: "Kingdom sliders trigger map regeneration"
---

<objective>
Implement Kingdom border generation using Voronoi cells expanded by terrain cost, driven by Poisson Disc sampling for capital placement.

Purpose: Fulfill decisions D-13 through D-17. Produce organic, terrain-respecting political boundaries that do not claim ocean tiles.
Output: A backend algorithm that groups `MapCell`s into `Kingdom`s, rendered over the base terrain, with UI controls for kingdom count and relaxation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-map-generation/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kingdom Data Model and Poisson Disc Sampling</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/core/map/Kingdom.java, src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java</files>
  <action>Create a `Kingdom` class holding an ID, color, and central capital cell reference. In `MapGenerator`, implement a Poisson Disc sampling algorithm to place the requested number of `Kingdom` capitals on valid land tiles (elevation >= WaterLevel), preventing clumping.</action>
  <verify>
    <automated>mvn test</automated>
  </verify>
  <done>Kingdom capitals are distributed evenly across landmasses.</done>
</task>

<task type="auto">
  <name>Task 2: Cost-based Voronoi Expansion and Sea Claims</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java</files>
  <action>Implement a Dijkstra/A* expansion starting from the capitals. The terrain cost should be high for mountains and prohibit traversal across ocean tiles (D-16). Assign each visited land `MapCell` a reference to its owning `Kingdom`.</action>
  <verify>
    <automated>mvn test</automated>
  </verify>
  <done>Every land tile is claimed by exactly one Kingdom, respecting natural terrain boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Lloyd's Relaxation and UI Integration</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java, src/main/java/com/mapbuilder/mapbuilder/main/MainView.java, src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java, src/main/java/com/mapbuilder/mapbuilder/main/MainModel.java</files>
  <action>Implement Lloyd's relaxation: recalculate the centroid of each Kingdom's cells and restart the Voronoi expansion. Repeat this for the number of configured passes (D-15). Add UI sliders in `MainView` for "Kingdom Count" and "Lloyd Passes (0-5)". Add toggles for Kingdom borders and tint overlay. Bind these controls in `MainPresenter` to trigger regeneration in `MainModel`.</action>
  <verify>
    <automated>mvn test</automated>
  </verify>
  <done>UI controls dynamically adjust the Kingdom generation, and relaxation visibly smooths the borders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI Slider Limits | Ensure Lloyd passes are capped to avoid CPU hangs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Denial of Service | MapGenerator | mitigate | Hard cap Lloyd passes to 5 in the backend regardless of UI input. |
</threat_model>

<verification>
Ensure Kingdoms only exist on land. Verify high terrain (mountains) correctly diverts border lines. Verify toggling layers on/off works instantly without full regeneration.
</verification>

<success_criteria>
Kingdom generation accurately implements D-13 to D-17, visually matching the requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-map-generation/02-05-SUMMARY.md`
</output>
