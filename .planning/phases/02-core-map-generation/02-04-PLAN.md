---
phase: 02-core-map-generation
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - "src/main/java/com/mapbuilder/mapbuilder/presentation/MapView.java"
  - "src/main/resources/styles.css"
autonomous: true
requirements: ["Functional Requirements-2"]

must_haves:
  truths:
    - "The Layer Selector UI component is visible on the screen."
    - "It visually matches the provided mock (Image 1)."
  artifacts:
    - path: "src/main/java/com/mapbuilder/mapbuilder/presentation/MapView.java"
      provides: "Layer selector UI implementation"
  key_links:
    - from: "src/main/java/com/mapbuilder/mapbuilder/presentation/MapView.java"
      to: "src/main/resources/styles.css"
      via: "CSS classes for the layer selector"
---

<objective>
Implement the Layer Selector UI component as shown in the provided mock (Image 1).
Purpose: Provide users with a visual tool to select and manage different map layers.
Output: A fully styled and structured Layer Selector integrated into the MapView.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-map-generation/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Layer Selector Component</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/presentation/MapView.java</files>
  <action>Extend the UI to include the Layer Selector component as depicted in Image 1. Implement the structural elements (e.g., list, dropdown, or custom container) required to select active layers. Ensure it integrates cleanly with the existing layout.</action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <done>The MapView contains the structural elements for the Layer Selector.</done>
</task>

<task type="auto">
  <name>Task 2: Style Layer Selector</name>
  <files>src/main/resources/styles.css</files>
  <action>Add the necessary CSS classes to style the Layer Selector to match the mock (Image 1). This includes background colors, borders, typography, and active/inactive states.</action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <done>The Layer Selector matches the visual design of the mock.</done>
</task>

</tasks>

<verification>
Run the application and verify that the Layer Selector is visible and matches the provided mock design visually.
</verification>

<success_criteria>
The Layer Selector is implemented and styled according to the mock.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-map-generation/02-04-SUMMARY.md`
</output>
