---
phase: 02-core-map-generation
plan: 04
type: execute
wave: 1
depends_on: ["02-03"]
files_modified: ["src/main/java/com/mapbuilder/mapbuilder/main/MainView.java", "src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java", "src/main/java/com/mapbuilder/mapbuilder/main/MainModel.java", "src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java"]
autonomous: true
requirements: ["MAP-01", "MAP-02"]
must_haves:
  truths:
    - Hydrology controls exist in a separate tab from Terrain sliders
    - Tweaking river or lake settings triggers a debounced map regeneration
    - Master toggles can entirely disable rivers and lakes independently
    - Rivers and lakes can scale from 0% to 200% based on sliders
    - Lakes can generate independently of rivers forming in local valleys
  artifacts:
    - path: "src/main/java/com/mapbuilder/mapbuilder/main/MainView.java"
      provides: "Hydrology Tab with 2 toggles and 3 sliders"
    - path: "src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java"
      provides: "Configurable hydrology and independent lake pooling logic"
  key_links:
    - from: "src/main/java/com/mapbuilder/mapbuilder/main/MainView.java"
      to: "src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java"
      via: "Listener property binding to trigger generation pipeline"
---

<objective>
Implement fully configurable rivers and lakes with a dedicated Hydrology UI and independent lake generation.

Purpose: Complete context decisions D-11 through D-16, giving the user full control over hydrological generation including scaling, toggles, and independent lake pooling.
Output: Separate Hydrology tab in the UI, bound to the debounced generator, executing configurable rivers and lakes on the CPU generator backend.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-map-generation/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hydrology Tab and UI Controls</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainView.java</files>
  <action>Refactor the control panel into a `TabPane`. Move existing terrain sliders into a "Terrain" tab. Create a new "Hydrology" tab. Inside Hydrology, add master toggles (`ToggleButton` or `CheckBox`) for "Enable Rivers" and "Enable Lakes". Add sliders for "River Density" (0% to 200%, default 100%), "Lake Size Multiplier" (0% to 200%, default 100%), and "Min Lake Area" (integer slider, default to existing constant). Provide getter methods for all these new controls.</action>
  <verify>
    <automated>mvn test</automated>
  </verify>
  <done>UI correctly displays Terrain and Hydrology tabs with specified toggles and sliders.</done>
</task>

<task type="auto">
  <name>Task 2: Bind Hydrology Controls to Generation</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java, src/main/java/com/mapbuilder/mapbuilder/main/MainModel.java</files>
  <action>Add listeners to the new Hydrology controls in `MainPresenter`. Bind their state to matching properties in `MainModel`. Ensure changing any of these 5 controls fires the same debounced `requestMapGeneration` logic that the terrain sliders use. Pass the new parameters from `MainModel` into `MapGenerator.generate()`.</action>
  <verify>
    <automated>mvn test</automated>
  </verify>
  <done>Hydrology changes actively trigger map regeneration on the canvas.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Configurable Logic and Independent Lakes</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java</files>
  <action>Update `MapGenerator.generate()` to accept the 5 new hydrology parameters. Scale `RIVER_DENSITY_DIVISOR` and `LAKE_AREA_RATIO` by the user's multipliers. If river toggle is off or density is 0%, skip river generation. If lake toggle is on, allow lakes. Modify the lake logic to allow **Independent Lakes** (D-16): if lakes are enabled but rivers are skipped or low density, implement a pass that finds local elevation minima on land and pools water outwards to the configured size constraints.</action>
  <verify>
    <automated>mvn test</automated>
  </verify>
  <done>MapGenerator respects all master toggles, scale multipliers, and forms independent lakes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI Slider Limits | User input validation for CPU algorithms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Denial of Service | MapGenerator | mitigate | Cap the maximum size and density scales at 200% to avoid CPU stall during independent lake pooling algorithms. |
</threat_model>

<verification>
Ensure toggling off rivers removes rivers, and toggling off lakes removes lakes. Verify lakes still pool in deep valleys even when rivers are toggled off. Verify 200% slider creates more/larger lakes and rivers.
</verification>

<success_criteria>
Hydrology UI exists and behaves predictably, fulfilling D-11 to D-16.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-map-generation/02-04-SUMMARY.md`
</output>