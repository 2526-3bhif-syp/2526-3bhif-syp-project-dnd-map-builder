---
phase: 02-core-map-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/main/java/com/mapbuilder/mapbuilder/core/map/Biome.java"
  - "src/main/java/com/mapbuilder/mapbuilder/core/map/MapCell.java"
  - "src/main/java/com/mapbuilder/mapbuilder/core/map/MapGrid.java"
  - "src/main/java/com/mapbuilder/mapbuilder/core/math/FastNoiseLite.java"
  - "src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java"
autonomous: true
requirements: ["Functional Requirements-1", "Functional Requirements-2"]

must_haves:
  truths:
    - "A core data structure (MapGrid) holds elevation, temperature, rainfall, and biomes."
    - "16 distinct Biomes exist and map to numeric combinations."
    - "A noise generator produces the map grid mathematically."
  artifacts:
    - path: "src/main/java/com/mapbuilder/mapbuilder/core/map/MapGrid.java"
      provides: "Decoupled 2D grid of MapCells"
    - path: "src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java"
      provides: "Generates the map using Perlin noise/fBm"
  key_links:
    - from: "src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java"
      to: "src/main/java/com/mapbuilder/mapbuilder/core/math/FastNoiseLite.java"
      via: "fBm noise computation"
---

<objective>
Implement the backend Map Generation engine and core math.
Purpose: Provide a decoupled, pure-Java data structure and generation logic for the map using FastNoiseLite and Perlin/fBm noise, preparing 16 biomes per Whittaker diagram logic.
Output: MapGrid, MapCell, Biome enum, FastNoiseLite, and MapGenerator.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-map-generation/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Data Structures</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/core/map/Biome.java, src/main/java/com/mapbuilder/mapbuilder/core/map/MapCell.java, src/main/java/com/mapbuilder/mapbuilder/core/map/MapGrid.java</files>
  <action>Create the Biome enum with 16 distinct biomes and their color constants (D-07). Create MapCell holding x, y, elevation, temperature, rainfall, and a Biome reference (D-01). Create MapGrid to hold a 2D array of MapCells. Ensure it is fully decoupled from JavaFX (D-02). Pre-allocate the 2D array to avoid object thrashing.</action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <done>Biome enum and MapGrid/MapCell classes compile and exist.</done>
</task>

<task type="auto">
  <name>Task 2: Noise Engine and Generator</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/core/math/FastNoiseLite.java, src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java</files>
  <action>Add FastNoiseLite (Java port) for fBm noise (D-03). Create MapGenerator that builds the elevation, temperature, and rainfall fields using fBm, Island_Falloff_Mask, and Altitude_Cooling (D-04, D-05, D-06). Map the fields to the 16 Biomes using Whittaker logic (D-08). The generator should update existing MapCell properties in MapGrid rather than instantiating new ones for efficiency.</action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <done>MapGenerator can take a MapGrid and seed, populating the grid's cells with correct primitive values and biomes.</done>
</task>

</tasks>

<verification>
Automated builds succeed and the core backend is structurally sound.
</verification>

<success_criteria>
The backend data model and noise engine are fully implemented without JavaFX dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-map-generation/02-01-SUMMARY.md`
</output>
