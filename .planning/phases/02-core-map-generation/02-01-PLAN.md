---
phase: 02-core-map-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/main/java/com/mapbuilder/mapbuilder/core/math/FastNoiseLite.java"
  - "src/main/java/com/mapbuilder/mapbuilder/core/map/Biome.java"
  - "src/main/java/com/mapbuilder/mapbuilder/core/map/MapCell.java"
  - "src/main/java/com/mapbuilder/mapbuilder/core/map/MapGrid.java"
  - "src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java"
autonomous: true
requirements:
  - REQ-01
  - REQ-02
must_haves:
  truths:
    - "Map generator deterministically outputs structured geographic data based on seed"
    - "Map contains varied biomes based on environmental factors like elevation, temperature, and rainfall"
    - "Generated maps have coherent terrain features with smooth natural transitions"
  artifacts:
    - path: "src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java"
      provides: "Map generation logic and biome resolution"
      contains: "public MapGrid generate"
    - path: "src/main/java/com/mapbuilder/mapbuilder/core/math/FastNoiseLite.java"
      provides: "Procedural noise calculations"
      contains: "public class FastNoiseLite"
  key_links:
    - from: "src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java"
      to: "src/main/java/com/mapbuilder/mapbuilder/core/math/FastNoiseLite.java"
      via: "Noise generation instances"
      pattern: "new FastNoiseLite"
---

<objective>
Implement the backend Map Generation engine utilizing FastNoiseLite, creating a decoupled `MapGrid` composed of `MapCell`s containing elevation, temperature, rainfall, resolved `Biome` data, and blended transition colors.

Purpose: To create the core mathematical data structure for the maps (D-01 to D-08), independent of UI concerns, supporting reproducible seeded random generation.
Output: Core data models and the `MapGenerator` service capable of computing the fields and Whittaker biome lookup, including wind logic, ocean temperature moderation, and color mixing.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-core-map-generation/02-CONTEXT.md
@.planning/phases/02-core-map-generation/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Map Data Structures & Noise Engine Setup</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/core/math/FastNoiseLite.java, src/main/java/com/mapbuilder/mapbuilder/core/map/Biome.java, src/main/java/com/mapbuilder/mapbuilder/core/map/MapCell.java, src/main/java/com/mapbuilder/mapbuilder/core/map/MapGrid.java</files>
  <read_first>
    - .planning/phases/02-core-map-generation/02-CONTEXT.md
  </read_first>
  <action>
    Create the mathematical base and core models:
    
    1. Download/Create `FastNoiseLite.java` in `core/math/` (standard open-source Java port, or placeholder implementation that generates valid Perlin fBm noise). It must provide a `GetNoise(x, y)` method.
    2. Create `Biome` enum in `core/map/` with exactly 16 values (D-07): DEEP_OCEAN(0xFF00008B), OCEAN(0xFF0000CD), SHALLOW_SEA(0xFF4169E1), BEACH(0xFFF4A460), SCORCHED(0xFF555555), BARE_ROCK(0xFF888888), TUNDRA(0xFFAAAAAA), SNOW_PEAKS(0xFFFFFFFF), SHRUBLAND(0xFF88AA55), GRASSLAND(0xFF7CFC00), SAVANNA(0xFFBDB76B), DESERT(0xFFEDC9AF), TEMPERATE_FOREST(0xFF228B22), TEMPERATE_RAINFOREST(0xFF006400), TROPICAL_DRY_FOREST(0xFF556B2F), TROPICAL_RAINFOREST(0xFF004B23). Each must store its base color ARGB integer and have a `getColorARGB()` method.
    3. Create `MapCell` class with `int x`, `int y`, `double elevation`, `double temperature`, `double rainfall`, `Biome biome`, and `int mixedColorARGB` (for biome color transitions). Provide getters/setters.
    4. Create `MapGrid` class encapsulating a `MapCell[][]` 2D array, with `int width` and `int height`. Initialize all `MapCell`s in the constructor to prevent object thrashing during updates. Add a `getCell(x, y)` method.
  </action>
  <verify>
    <automated>find src/main/java/com/mapbuilder/mapbuilder/core -name "*.java" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/mapbuilder/mapbuilder/core/map/Biome.java` contains exactly 16 enum constants matching the action list.
    - `MapGrid` constructor pre-allocates all `MapCell`s in a 2D array.
    - `MapCell.java` contains `public int getMixedColorARGB()`.
  </acceptance_criteria>
  <done>Core data models and noise engine are available for the generator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map Generator & Biome Resolution</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java, src/test/java/com/mapbuilder/mapbuilder/core/map/MapGeneratorTest.java</files>
  <read_first>
    - src/main/java/com/mapbuilder/mapbuilder/core/map/MapGrid.java
    - src/main/java/com/mapbuilder/mapbuilder/core/map/Biome.java
  </read_first>
  <behavior>
    - Generator initializes correctly with a seed.
    - generate(grid, waterLevel, seed) populates elevation, temp, rainfall values and assigns non-null Biomes to every cell.
    - Ocean temp moderation normalizes temperature near coasts.
    - Wind logic applied to rainfall (rain shadow effect).
  </behavior>
  <action>
    Create `MapGenerator`:
    1. Define public method `public void generate(MapGrid grid, int seed, double waterLevel, double temperatureBias, double rainfallBias)`.
    2. Configure `FastNoiseLite` instances (elevation, temperature, rainfall, wind) using the `seed`.
    3. Compute fields per cell:
       - `elevation`: `noiseVal + Island_Falloff_Mask`.
       - `temperature`: `noiseVal + Latitude_Gradient(y) - Altitude_Cooling(elevation) + temperatureBias`. Add Ocean Temp Moderation: if elevation is near waterLevel, pull temperature towards a moderate baseline.
       - `rainfall`: `noiseVal + Inverse_Temperature_Bias(temperature) + rainfallBias`. Add Wind Logic: Cast a ray from West to East (x-1 to x); if elevation is rising sharply, increase rainfall (windward); if dropping, decrease rainfall (leeward rain shadow).
       - Ensure NO RIVERS are generated.
    4. Resolve Biome (D-08): Evaluate Whittaker grid based on elevation, temp, rain. Set `cell.setBiome(biome)`.
    5. Color Mixing: Based on the exact temp/rainfall values compared to the biome's center, calculate a blended `mixedColorARGB` with neighbors and `cell.setMixedColorARGB(...)`. This replaces discrete extra biomes for transitions.
  </action>
  <verify>
    <automated>./gradlew test --tests *MapGeneratorTest*</automated>
  </verify>
  <acceptance_criteria>
    - `MapGenerator.java` contains `public void generate(MapGrid grid, int seed, double waterLevel, double temperatureBias, double rainfallBias)`
    - Generator implementation calculates `mixedColorARGB` based on biome color blending.
    - Test executes and passes, proving generation modifies cells in-place and handles island falloff.
  </acceptance_criteria>
  <done>The MapGenerator successfully populates a pre-allocated MapGrid with advanced climate features.</done>
</task>

</tasks>

<verification>
- Models exist in the correct `core/map` structure.
- Math is fully decoupled from JavaFX / MVP layers.
</verification>

<success_criteria>
- Build and tests pass.
- MapGrid structure avoids object thrashing by re-using MapCell instances.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-map-generation/02-01-SUMMARY.md`
</output>