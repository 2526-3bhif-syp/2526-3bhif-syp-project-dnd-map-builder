---
phase: 02-core-map-generation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - "src/main/java/com/mapbuilder/mapbuilder/presentation/MapModel.java"
  - "src/main/java/com/mapbuilder/mapbuilder/presentation/MapPresenter.java"
  - "src/main/java/com/mapbuilder/mapbuilder/presentation/MapView.java"
autonomous: true
requirements: ["Functional Requirements-1", "Functional Requirements-2"]

must_haves:
  truths:
    - "Map generation is debounced from UI inputs."
    - "The generated MapGrid is rendered onto the JavaFX Canvas."
    - "Slider interactions update the Canvas smoothly without UI locking."
  artifacts:
    - path: "src/main/java/com/mapbuilder/mapbuilder/presentation/MapPresenter.java"
      provides: "Binds model generation with view interaction and rendering."
    - path: "src/main/java/com/mapbuilder/mapbuilder/presentation/MapView.java"
      provides: "JavaFX UI setup and slider inputs."
  key_links:
    - from: "src/main/java/com/mapbuilder/mapbuilder/presentation/MapPresenter.java"
      to: "src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java"
      via: "Asynchronous task execution"
---

<objective>
Bind MapGenerator to UI, implement debounced rendering and MapGrid CPU Translation Layer to Canvas.
Purpose: Render the backend grid onto the JavaFX screen in a non-blocking way based on interactive slider inputs.
Output: Integrated Presenter logic, updated View sliders, WritableImage logic.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-map-generation/02-CONTEXT.md
@.planning/phases/02-core-map-generation/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CPU Translation and Rendering</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/presentation/MapPresenter.java, src/main/java/com/mapbuilder/mapbuilder/presentation/MapView.java, src/main/java/com/mapbuilder/mapbuilder/presentation/MapModel.java</files>
  <action>Implement the CPU Translation Layer (D-09): iterate over the MapGrid, map Biome instances to their ARGB int colors, and pack them into a flat int array. Write this array in bulk using PixelWriter.setPixels to a WritableImage. Draw this image onto the Canvas in the View. MapModel holds the MapGrid and coordinates calls to the generator.</action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <done>PixelWriter efficiently renders the map without using setColor().</done>
</task>

<task type="auto">
  <name>Task 2: UI Binding and Debouncing</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/presentation/MapPresenter.java, src/main/java/com/mapbuilder/mapbuilder/presentation/MapView.java</files>
  <action>Add sliders/inputs in MapView for Size, Water %, etc. Use JavaFX PauseTransition (300ms) in MapPresenter to debounce the inputs (D-10). Execute MapGenerator via a JavaFX Task to prevent UI thread blocking. Ensure sliders normalize 0-100 inputs to noise thresholds before sending them to MapGenerator.</action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <done>Sliders trigger background map generation and refresh the canvas smoothly.</done>
</task>

</tasks>

<verification>
Start the application; moving sliders updates the canvas correctly in real-time.
</verification>

<success_criteria>
The noise-generated map is fully integrated into the MVP's visual canvas.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-map-generation/02-02-SUMMARY.md`
</output>
