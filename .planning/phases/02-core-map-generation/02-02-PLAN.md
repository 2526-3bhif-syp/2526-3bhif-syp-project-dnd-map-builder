---
phase: 02-core-map-generation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - "src/main/java/com/mapbuilder/mapbuilder/main/MainModel.java"
  - "src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java"
  - "src/main/java/com/mapbuilder/mapbuilder/main/MainView.java"
autonomous: true
requirements:
  - REQ-01
  - REQ-02
must_haves:
  truths:
    - "UI provides interactive controls for map parameters"
    - "Map generation executes asynchronously and debounces input"
    - "The generated geographic map is visually presented to the user on the primary canvas"
  artifacts:
    - path: "src/main/java/com/mapbuilder/mapbuilder/main/MainView.java"
      provides: "Sliders for Size, Water Level, Temperature, Rainfall, Seed"
      contains: "new Slider"
    - path: "src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java"
      provides: "Debounced rendering logic"
      contains: "new PauseTransition"
  key_links:
    - from: "src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java"
      to: "src/main/java/com/mapbuilder/mapbuilder/main/MainModel.java"
      via: "Background task"
      pattern: "Task<Void>"
---

<objective>
Bind the MapGenerator to the MainView sliders and implement high-performance rendering.

Purpose: Translate the backend `MapGrid` into a visual JavaFX `WritableImage` and allow real-time (debounced) parameter tweaking.
Output: Integrated map rendering with a responsive UI.
</objective>

<execution_context>
@/home/nico/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-core-map-generation/02-CONTEXT.md
@.planning/phases/02-core-map-generation/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Map Generation UI Controls</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainView.java, src/main/java/com/mapbuilder/mapbuilder/core/MVPBase.java</files>
  <read_first>
    - src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
  </read_first>
  <action>
    Update MVP View to include required parameters:
    1. In `MainView.java`, locate the `leftPanel` creation and modify the dummy elements (Seed..., Slider, Generate) to:
       - `TextField seedField` (for map seed)
       - `Slider waterLevelSlider` (-0.5 to 0.5, default 0.0)
       - `Slider tempBiasSlider` (-1.0 to 1.0, default 0.0)
       - `Slider rainBiasSlider` (-1.0 to 1.0, default 0.0)
       - Include `Label`s above each slider (e.g., "Water Level", "Temperature", "Rainfall").
    2. Add getters for these UI elements to the `View` interface in `MVPBase.java` (e.g., `TextField getSeedField()`, `Slider getWaterLevelSlider()`, etc.) and implement them in `MainView`.
    3. Ensure `MainView` returns the existing `canvas` via a new `Canvas getCanvas()` method in the `View` interface.
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - `MVPBase.java` interface `View` contains `Slider getWaterLevelSlider()`, `TextField getSeedField()`, and `Canvas getCanvas()`.
    - `MainView.java` successfully implements these methods and adds the labels/sliders to `leftPanel`.
  </acceptance_criteria>
  <done>The View presents the required map generation parameters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Debouncer and Async Map Generation</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainModel.java, src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java, src/test/java/com/mapbuilder/mapbuilder/main/MainPresenterTest.java</files>
  <read_first>
    - src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java
    - src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java
  </read_first>
  <behavior>
    - Property listeners successfully trigger debouncer instead of immediate rendering.
    - Generation runs asynchronously (using Task or similar abstraction).
  </behavior>
  <action>
    Wire the Presenter to generate maps dynamically (D-09, D-10):
    1. In `MainModel.java`, add a `MapGrid currentGrid` (instantiated at e.g., 500x500 for now) and a `MapGenerator` instance. Add a method `public void generateMap(int seed, double waterLevel, double tempBias, double rainBias)` that delegates to `MapGenerator.generate()`.
    2. In `MainPresenter.java`, instantiate a `PauseTransition debounce = new PauseTransition(Duration.millis(300));`.
    3. Create a private method `triggerGeneration()`:
       - Stop the debounce if running, `playFromStart()`.
       - On `debounce.setOnFinished(...)`: Read the values from the View's sliders/seed, then create a JavaFX `Task<Void>`.
       - In `Task.call()`, invoke `model.generateMap(...)`.
       - In `Task.setOnSucceeded(...)`, invoke a new private method `renderMap()`.
       - Run the task on a new Thread `new Thread(task).start()`.
    4. Bind `triggerGeneration()` to `addListener` on the View's `waterLevelSlider.valueProperty()`, `tempBiasSlider.valueProperty()`, `rainBiasSlider.valueProperty()`, and `seedField.textProperty()`.
    5. Write a simple mock-based test in `MainPresenterTest.java` that asserts the debouncer is utilized.
  </action>
  <verify>
    <automated>./gradlew test --tests *MainPresenterTest*</automated>
  </verify>
  <acceptance_criteria>
    - `MainPresenter.java` uses `PauseTransition` with `Duration.millis(300)`.
    - `MainPresenter.java` uses `javafx.concurrent.Task` to run `model.generateMap()` off the UI thread.
    - Property listeners call `debounce.playFromStart()`.
  </acceptance_criteria>
  <done>Parameter changes trigger background generation via debouncing.</done>
</task>

<task type="auto">
  <name>Task 3: Bulk Pixel Rendering Layer</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java</files>
  <read_first>
    - src/main/java/com/mapbuilder/mapbuilder/main/MainModel.java
    - src/main/java/com/mapbuilder/mapbuilder/core/map/MapGrid.java
  </read_first>
  <action>
    Implement CPU Translation Layer (D-09):
    1. In `MainPresenter.java`, inside the `renderMap()` method (called on the UI thread when the Task succeeds):
    2. Fetch the updated `MapGrid` from `MainModel`.
    3. Retrieve the `Canvas` from the View. Get its `GraphicsContext` and `PixelWriter`.
    4. Create an `int[] pixels = new int[grid.getWidth() * grid.getHeight()];`.
    5. Iterate through `grid` (nested `y`, then `x` loops):
       - Access the `MapCell` at `x,y`.
       - Fetch `cell.getMixedColorARGB()`.
       - Assign to `pixels[y * grid.getWidth() + x]`.
    6. Write the pixels back using: `pixelWriter.setPixels(0, 0, grid.getWidth(), grid.getHeight(), javafx.scene.image.PixelFormat.getIntArgbPreInstance(), pixels, 0, grid.getWidth());`.
  </action>
  <verify>
    <automated>./gradlew classes</automated>
  </verify>
  <acceptance_criteria>
    - `MainPresenter.java` instantiates an `int[]` array based on grid width and height.
    - Uses `PixelFormat.getIntArgbPreInstance()` to interpret colors.
    - Invokes `setPixels` on the `Canvas` `PixelWriter` exactly once per map generation cycle.
  </acceptance_criteria>
  <done>Generated grids translate directly to visible images on the canvas in bulk.</done>
</task>

</tasks>

<verification>
- View sliders exist and have correct bounds.
- UI debounces effectively, ensuring responsive inputs.
- High-performance PixelWriter avoids lag during map rendering.
</verification>

<success_criteria>
- Project compiles cleanly.
- Application displays generated map instantly upon launch and reacts to slider changes without blocking the UI thread.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-map-generation/02-02-SUMMARY.md`
</output>
