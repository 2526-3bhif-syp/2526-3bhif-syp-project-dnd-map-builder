---
phase: 02-core-map-generation
plan: 03
type: execute
wave: 1
depends_on: ["02-01", "02-02"]
files_modified:
  - src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java
  - src/main/java/com/mapbuilder/mapbuilder/core/map/MapGrid.java
  - src/main/java/com/mapbuilder/mapbuilder/core/map/MapCell.java
  - src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
  - src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java
autonomous: true
requirements: ["MAP-01"]
must_haves:
  truths:
    - User can generate maps with downward-flowing rivers
    - Rivers pool into lakes when they reach local minima before reaching the ocean
    - The UI contains controls for river generation parameters
  artifacts:
    - path: src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java
      provides: River and lake generation logic
    - path: src/main/java/com/mapbuilder/mapbuilder/core/map/MapCell.java
      provides: IsRiver and IsLake properties
  key_links:
    - from: src/main/java/com/mapbuilder/mapbuilder/main/MainView.java
      to: src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java
      via: UI parameter binding
---

<objective>
Implement downward-flowing rivers and lakes on the generated map.

Purpose: Rivers add realistic hydrological features to the map. Lakes form naturally in depressions.
Output: MapGenerator updated to spawn rivers that flow downhill and form lakes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java
@src/main/java/com/mapbuilder/mapbuilder/core/map/MapCell.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add River and Lake properties to MapCell</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/core/map/MapCell.java</files>
  <action>Add boolean properties `isRiver` and `isLake` to MapCell. Add appropriate getters and setters. Update rendering logic in MainModel/MainView to draw rivers (blue lines/pixels) and lakes (blue areas).</action>
  <verify>
    <automated>mvn test</automated>
  </verify>
  <done>MapCell supports river and lake state.</done>
</task>

<task type="auto">
  <name>Task 2: Implement River Generation Algorithm</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/core/map/MapGenerator.java</files>
  <action>Add a post-processing step to MapGenerator. Select random high-elevation, high-rainfall cells as river sources. Trace the path downwards (steepest neighbor). Mark path as river. If it hits a local minimum above sea level, fill neighbors with water (mark as lake) until it spills over or reaches maximum lake size.</action>
  <verify>
    <automated>mvn test</automated>
  </verify>
  <done>Rivers flow downhill and form lakes.</done>
</task>

<task type="auto">
  <name>Task 3: Add UI Controls for Rivers</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/main/MainView.java, src/main/java/com/mapbuilder/mapbuilder/main/MainPresenter.java</files>
  <action>Add a slider for "River Count" to the MainView. Bind it in MainPresenter to update the generator parameters and trigger a re-render.</action>
  <verify>
    <automated>mvn test</automated>
  </verify>
  <done>User can control the number of rivers generated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> UI | Local user inputs parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | MapGenerator | mitigate | Limit maximum river count and map size to prevent infinite loops in pathfinding or OOM errors. |
</threat_model>

<verification>
Check if rivers flow downhill and lakes form in depressions. Verify UI updates the river count.
</verification>

<success_criteria>
Map renders with rivers and lakes based on the generated terrain.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-map-generation/02-03-SUMMARY.md`
</output>
