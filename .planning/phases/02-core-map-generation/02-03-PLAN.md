---
phase: 02-core-map-generation
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - "src/main/java/com/mapbuilder/mapbuilder/presentation/MapView.java"
  - "src/main/java/com/mapbuilder/mapbuilder/presentation/MapPresenter.java"
autonomous: true
requirements: ["Functional Requirements-1", "Functional Requirements-2"]

must_haves:
  truths:
    - "The Layer Panel UI appears on the right edge of the screen."
    - "It has specific rounded rows with toggle icons."
    - "It has a pull-out tab for collapsing/expanding."
  artifacts:
    - path: "src/main/java/com/mapbuilder/mapbuilder/presentation/MapView.java"
      provides: "Adds the Layer Panel styling, rows, and expand/collapse button."
  key_links:
    - from: "src/main/java/com/mapbuilder/mapbuilder/presentation/MapView.java"
      to: "src/main/java/com/mapbuilder/mapbuilder/presentation/MapPresenter.java"
      via: "UI Toggle interaction"
---

<objective>
Implement and style the right-side Layer Panel per user sketch.
Purpose: Allow the user to toggle visual layers like Grid, Mountains, Rivers, POIs.
Output: Integrated Layer Panel in MapView with proper CSS styling.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-map-generation/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Layer Panel UI Structure</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/presentation/MapView.java, src/main/resources/styles.css</files>
  <action>Create the floating right-side Layer Panel (D-11). It must be a VBox/StackPane styled with a dark background. Implement a pull-out tab with `<` icon on the left edge. Build rows for "Markierungen", "Punkte von Interesse", "Strukturen & Straßen", "Berge", "Flüsse und Seen", and "Grid" (D-12). Each row gets an eye toggle icon aligned to the right (crossed-out when hidden). Use JavaFX Region constraints for rounded-rectangle shapes.</action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <done>The panel UI displays exactly as specified in the UI sketch, with styled buttons and rows.</done>
</task>

<task type="auto">
  <name>Task 2: Layer Interaction Logic</name>
  <files>src/main/java/com/mapbuilder/mapbuilder/presentation/MapView.java, src/main/java/com/mapbuilder/mapbuilder/presentation/MapPresenter.java</files>
  <action>Implement the expand/collapse animation or state change for the Layer Panel. Wire up the eye toggle icons to properties in the MapPresenter or Model. Note: The actual hiding of POIs/Grids will happen in future phases, but the toggle states (boolean properties) must be registered and trigger a canvas repaint.</action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <done>Clicking the eye icon updates the UI state (e.g., strike-through icon) and updates a boolean property.</done>
</task>

</tasks>

<verification>
Start application; right-side panel has 6 styled layers, toggle switches function correctly.
</verification>

<success_criteria>
Layer Panel matches the sketch visually and logically sets the groundwork for Phase 3/4 interactions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-map-generation/02-03-SUMMARY.md`
</output>
